# To install these Python dependencies, please type:
# > pip install -r requirements.txt
# or if you use conda:
# > conda install --file requirements.txt

# if installing using pip, uncomment faiss-cpu (NOT needed for building docker image. Will return "Illegal instruction" error if used). If building docker image then comment faiss-cpu (Dockerfile will download faiss separately)
faiss-cpu 
streamlit
tensorflow>=1.15
numpy
matplotlib
scipy
scikit-learn
pandas
pillow
pyyaml


# other notes: 
# if faiss-cpu fails to install using default channels, use
# conda install -c pytorch faiss-cpu


